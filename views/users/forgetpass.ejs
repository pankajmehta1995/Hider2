<!-- Three -->

<div class="login-page same-page"  ">
 <div>
      <div class="login-field">   
              <div class="login-details">
              <h1>Forgot Password</h1>
              <div id="message"  style="margin:-20px 0 10px 0; width:100%;">
               <font color='green'> <%= messages.successmsg; %> </font> 
              <font color='red'> <%= messages.errormsg; %> </font> 
               
            </div>
                 <form class="form-detail" name="forgetpass" id="forgetpass" method = "post" action="">
                    <div class="form-group">
                    <input type="text" name="email" id="email" placeholder=" Registered Email Address" class="inp-detail form-control"
                     <%if(users!=0) {%>
                       value="<%= users; %>" 
                     <%}%> >
                    </div> 

                     <div class="form-group">
                      <input type="submit" value="Submit" />
                     </div>             
                  </form>
        
                   
                </div>
       </div>
    </div>
</div>

      

      <script type="text/javascript">
       $(document).ready(function(){
         $("#forgetpass").validate({
           
           errorElement: 'span', //default input error message container
           errorClass: 'help-block', // default input error message class
           focusInvalid: false, // do not focus the last invalid input
           ignore: [],
           debug: true,
           rules: {
             email: {
               required:true,
               email:true
             },

           },
            messages: {
             email: {
               required: "Please enter Email Id",
               email: "You have not given a correct e-mail address"
             }
           },
           invalidHandler: function (event, validator) { //display error alert on form submit  
             $('.alert-danger', $('#login')).show();
           },      highlight: function (element) { // hightlight error inputs
             $(element)
               .closest('.form-group').addClass('has-error'); // set error class to the control group
           },      success: function (label) {
             label.closest('.form-group').removeClass('has-error');
             label.remove();
           },      errorPlacement: function (error, element) {
             error.insertAfter(element.closest('.form-control'));
           },      submitHandler: function (form) {
             form.submit();
           }
         });
       });
    </script> 
    <!-- style="min-height: calc( 100vh - 60px);background: url(/frontend/images/fp.jpg) no-repeat scroll center center;text-align: center;
    position: relative; background-size:100% 100%; opacity: 0.8; -->
